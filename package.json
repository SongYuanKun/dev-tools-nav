{
  "name": "dev-tools-nav",
  "private": true,
  "scripts": {
    "test": "node --test scripts/*.test.mjs",
    "capture-screenshots": "node scripts/capture-screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "^1.51.0"
  }
}

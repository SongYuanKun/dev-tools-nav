# 安全政策与漏洞披露

## 安全声明

**Koen's 工具箱（dev-tools-nav）** 是一个纯静态前端项目。所有 10 款在线工具（JSON 工作台、时间戳、Cron、Base64、JWT、SQL、正则、UUID、Diff、颜色）均**完全在用户本地浏览器中运行**，不向任何服务端上传、保存、分析或回传用户输入的数据。项目不提供用户账号系统、不执行任何服务端代码、不使用 Cookie 进行身份识别，仅在用户主动同意的情况下通过 Umami 匿名统计页面访问次数以指导维护投入方向。

尽管如此，我们仍严肃对待所有安全漏洞报告，并承诺在合理时间内响应、评估并修复任何已确认的问题。

## 报告安全漏洞

请**不要**在公共 Issue 中报告潜在的安全漏洞。请通过以下私密渠道向维护者披露：

| 渠道 | 地址 |
|------|------|
| 电子邮件 | **SongYuanKun <123839070@qq.com>**（推荐，7×24 小时可达） |
| GitHub 私密讨论 | https://github.com/SongYuanKun/dev-tools-nav/discussions（标题注明「安全漏洞报告」） |

为了帮助我们更快地评估和复现问题，报告时请尽量提供：

1. 问题类型（XSS、数据泄露、CDN 供应链、第三方依赖 CVE 等）
2. 触发漏洞的最小可复现步骤或 PoC 代码片段
3. 受影响的版本范围（commit SHA 或标签）
4. 任何其他可能相关的环境信息（浏览器版本、操作系统等）

## 披露时间线

我们的标准漏洞处理流程如下：

| 阶段 | 目标时间 | 说明 |
|------|---------|------|
| **确认收到报告** | 48 小时内 | 维护者回复邮件，确认已收到并分配处理 |
| **初始评估与复现** | 5 个工作日内 | 判断严重等级（严重/高/中/低），确认是否可复现 |
| **修复开发与验证** | 视严重等级而定 | 严重 ≤ 14 天；高 ≤ 30 天；中/低按排期 |
| **发布修复与公告** | 修复合并当天 | 在 Release Notes / 博客 / 邮件通知中向报告者致谢 |

## 报告者致谢

若您报告的漏洞经确认属实并修复：

- 我们将在对应 Release 的致谢名单中署您的姓名或 ID（可匿名）
- 在安全公告末尾公开感谢（若您同意）
- 严重或高危漏洞将额外在项目 README 的「贡献者」章节永久记录

## 第三方依赖安全

项目使用的第三方 npm 依赖与 CDN 资源清单：

| 类型 | 清单位置 |
|------|----------|
| npm 依赖（构建与 CodeMirror） | [package.json](package.json) |
| 外部 CDN / favicon 远程图标 | [data/tools.js](data/tools.js) 的 `icon` 字段 |
| 站内统计脚本（Umami，可选） | [js/base.js](js/base.js) |

若您发现上游依赖存在已知 CVE 或供应链风险，同样欢迎按上述流程上报。

## 免责声明

本项目按 MIT 许可证「按现状」提供，不提供任何形式的担保。详尽免责条款请参阅 [LICENSE](https://github.com/SongYuanKun/dev-tools-nav/blob/main/LICENSE) 文件（blob/main 格式）。
